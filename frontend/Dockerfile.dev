FROM node:20.19-alpine3.21

WORKDIR /app

# Polling is required for hot-reload to work through Docker volume mounts on Windows/macOS
ENV CHOKIDAR_USEPOLLING=true
ENV WATCHPACK_POLLING=true

COPY package*.json ./
RUN npm install

EXPOSE 3000

CMD ["npm", "start"]
