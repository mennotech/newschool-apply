services:
  backend:
    build: ./backend
    ports:
      - "8080:80"
    volumes:
      - drupal_db:/var/www/html/web/sites/default/files
    environment:
      - DRUPAL_ADMIN_USER
      - DRUPAL_ADMIN_PASS
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-http://localhost:3000}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - REACT_APP_DRUPAL_BASE_URL=http://localhost:8080
    depends_on:
      - backend

volumes:
  drupal_db:
